export default class Helper {
    private reportKeys;
    createReadme: (path?: string) => boolean;
    insertBadges: (path?: string) => boolean;
    getBuildStatus: (path?: string) => boolean;
    private getCoveragePath;
    private getBadge;
    private getColor;
}
